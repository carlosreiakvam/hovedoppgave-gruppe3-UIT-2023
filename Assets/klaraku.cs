using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class klaraku : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        RelayConnector relayConnector = RelayConnector.Instance;
    }
}